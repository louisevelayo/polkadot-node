aws_cluster:
  hosts:
    pdn1:
      ansible_host: "your.ec2.host.domain"
      ansible_ssh_private_key_file: <insert path to private key>
    pdn2:
      ansible_host: "your.ec2.host.domain"
      ansible_ssh_private_key_file: <insert path to private key>
  vars:
    ansible_user: ubuntu